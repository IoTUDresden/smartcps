<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" >

    <Preference
        android:key="fake"
        android:layout="@layout/_settings_dev_add_new_service_btn" >
    </Preference>

    <Preference
        android:key="fake"
        android:layout="@layout/_settings_dev_reset_btn" >
    </Preference>

    <CheckBoxPreference
        android:defaultValue="false"
        android:key="settings_dev_enable_mock_data"
        android:summary="@string/settings_dev_enable_mock_data_summery"
        android:title="@string/settings_dev_enable_mock_data_title" />

</PreferenceScreen>